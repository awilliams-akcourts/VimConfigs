How to install on windows

- clone to %localappdata%/nvim

- clone packer.nvim to appdata directory (powershell command on packer.nvim github)

- install ripgrep (cargo install ripgrep) (requires rust)

- install zig 
- (for c compiler for nvim lsp and stuff, just download zip from website, extract anywhere and add it to path)

